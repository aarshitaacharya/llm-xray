# Direct dependencies only. The previous file was a full `pip freeze` of the
# dev environment, which dragged in ~450 transitive and unused packages
# (scikit-image among them) and bloated the container image.
fastapi==0.115.12
uvicorn[standard]==0.34.0
google-generativeai==0.8.5
numpy==1.26.4
python-dotenv==1.1.0
boto3==1.35.99
